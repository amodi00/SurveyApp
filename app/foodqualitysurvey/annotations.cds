using CafeteriaSurveySrv as service from '../../srv/service';

annotate service.Survey with @(
    UI.DeleteHidden : true
);
annotate service.Survey with @(
    UI.UpdateHidden : true
);
annotate service.Survey with @(
    UI.UpdateHidden : true
);
annotate service.Survey with @(
    UI.DeleteHidden : true
);
